# Переток ФЛ между ИНН: методика расчёта

Отчёт отвечает на вопрос «куда делись зарплатные клиенты организации за год»:
ушли из банка, перешли в другой ИНН, переехали в другое подразделение или
просто перестали проходить порог.

Код: `uzp_flow/`, запуск — `flow.ipynb`. Числа ниже получены запуском на
синтетике открытого контура (`python3 -m uzp_flow.synth`), а не взяты из головы.

---

## 1. Кто считается получателем

**Получатель** — четвёрка **(ТБ, ГОСБ, ИНН, ЕПК ID)** в конкретном месяце, как
задано заказчиком. Берутся `sys_tb_id` и `sys_gosb_id`, а не `tb_id`/`gosb_id`:
на проме `tb_id` в ведомостях пуст, а `gosb_id` со справочником не сходится —
отчёт, построенный на них, показал бы одну строку «ТБ неизвестен», не падая и
ничего не сообщая.

Человек попадает в портфель ИНН за месяц, если:

1. зачисление прошло по **зарплатному коду** — `enrollment_type` из списка
   `config.PAYROLL_CODES` (зарплата, аванс, премия, отпускные, денежное
   довольствие и т.д.). Пенсии, пособия, субсидии в портфель не входят;
2. **сумма всех таких зачислений за месяц по паре (ЕПК, ИНН)** строго больше
   порога `AMT_MIN` (по умолчанию 2 500 ₽).

Порог считается по **паре (человек, ИНН)**, а не по строке витрины: грейн
`uzp_data_payroll_m` — строка на каждый вид зачисления, и один человек за месяц
даёт несколько строк. Порог по строке дал бы другое число.

Порог считается **по паре**, а портфель — **по четвёрке**: человек, получающий
от одного ИНН через два подразделения, — это два получателя, но одна пара.

## 2. Что такое переток

Переток определяется **на уровне человека (ЕПК)**, без подразделения: переезд
между ГОСБ одного ИНН перетоком не является.

Для каждого ЕПК берутся два множества ИНН — базового месяца (S₀) и отчётного (S₁):

| Случай | Что это | Считается перетоком |
|---|---|---|
| ИНН A ∈ S₀, A ∉ S₁, и есть **новый** ИНН B ∈ S₁ \ S₀ | человек ушёл из A и появился в B | **да**, A → B |
| ИНН A ∈ S₀, A ∉ S₁, S₁ пусто | человек пропал из ведомостей вовсе | нет — уход |
| S₀ = {A, B}, S₁ = {B} | получал от двух ИНН, стал от одного | **нет** — уход с одного ИНН |
| S₀ = {A}, S₁ = {A, B} | добавился второй ИНН, первый не потерян | нет — A ничего не потерял |
| A ∈ S₀ ∩ S₁, сменился ГОСБ | переезд внутри ИНН | нет — отдельная колонка |

**ИНН-приёмник человека** — тот из новых ИНН, где сумма зачислений отчётного
месяца больше. Если новых ИНН у человека нет, он в расчёте перетока не участвует
вовсе.

**ИНН перетока организации** — приёмник, куда ушло больше всего её людей
(строка с `rn = 1`). Остальные направления лежат в файле `flow_edges_*.csv`,
до `TOP_N` штук на организацию.

Переток приписывается тому подразделению, **где человек числился в базовом
месяце**: потеряло его именно оно.

## 3. Массовый переход или ротация

Различаются **двумя условиями сразу**:

```
массовый переход  ⇔  (ФЛ в один ИНН ≥ MASS_MIN_QTY)  И
                     (ФЛ в один ИНН / портфель базового месяца ≥ MASS_MIN_SHARE)
ротация           ⇔  переток есть, но условие выше не выполнено
```

По умолчанию `MASS_MIN_QTY = 10`, `MASS_MIN_SHARE = 0.20`. Одного условия мало:
12 человек из 3 000 — это ротация, 12 из 15 — переезд организации.

## 4. Ротация портфеля

Считается **на грейне (ТБ, ГОСБ, ИНН)** — там же, где задана уникальность ФЛ:

```
Ушло ФЛ    = получателей базового месяца, которых нет в отчётном
Пришло ФЛ  = получателей отчётного месяца, которых не было в базовом
Ротация, % = Ушло / Численность базового месяца × 100
```

Знаменатель обоих процентов — портфель **базового** месяца: это та численность,
которая за год и обновлялась. У организаций, которых в базовом месяце не было
(портфель 0), доля не определена и остаётся пустой.

Отдельная колонка **«Ушло в другой ГОСБ того же ИНН»** — те, кто в грейне
(ТБ, ГОСБ, ИНН) выглядит ушедшим, не будучи им. Без неё ротация читается неверно.

## 5. Реальная потеря

```
Дельта за год             = Численность отч. − Численность базового
Реальная потеря ФЛ        = max(0, Численность базового − Численность отч.)
в т.ч. объяснено перетоком = min(Реальная потеря, Перетекло ФЛ)
```

Потеря — **нетто**, а не сумма ушедших: ротация гоняет людей в обе стороны, и
интересует изменение общего количества, а не судьба отдельных людей. Минимум, а
не сумма, во второй строке: перетёкших может оказаться больше нетто-потери, если
параллельно шёл приток.

## 6. Справочные атрибуты

| Поле | Источник | Фолбэк |
|---|---|---|
| Название | `uzp_dim_company.company_name` по ИНН | `company_name` из ведомостей отчётного месяца |
| Холдинг | `uzp_dim_company.holding_name` | пусто (на проме заполнен у ~72%) |
| Признак силовика | `bool_or(uzp_data_epk_consolidation.is_military)` по ИНН | `bool_or(payroll.is_security_force)` |
| ТБ, ГОСБ | `uzp_dim_gosb` по (tb_id, new_gosb_id) | пусто, код остаётся в колонке |

Признак силовика собирается через `bool_or`: у одного ИНН несколько ЕПК, и
признак стоит не на всех. `is_security_force` в ведомостях на проме пуста
(заполненность 0%), поэтому она именно фолбэк, а не основной источник.

**Склейка ИНН.** В ведомостях `inn` — TEXT, и в нём встречается нечисловое и
номера с ведущим нулём; в справочниках — bigint. Приводится **справочник к
TEXT**, а не ведомости к bigint: `CAST(inn AS bigint)` по колонке ведомостей
роняет запрос целиком, а не одну строку. Ведущие нули снимаются `ltrim(inn,'0')`
с обеих сторон.

## 7. Пример с числами (синтетика открытого контура)

Прогон `python3 -m uzp_flow.synth`, 17 сценариев, август-2026 к августу-2025:

| Показатель | Значение |
|---|---|
| Строк (ТБ+ГОСБ+ИНН) | 19 |
| Численность базового месяца | 402 |
| Численность отчётного месяца | 335 |
| Ушло ФЛ за год | 196 |
| Пришло ФЛ за год | 129 |
| Перетекло ФЛ в другие ИНН | 94 |
| Переехало между ГОСБ одного ИНН | 15 |
| Организаций с массовым переходом | 1 |
| Организаций с ротацией | 3 |
| Реальная потеря ФЛ | 192 |

Разбор одной строки — ИНН `7700000001`, сценарий «массовый переход»:

| Колонка | Значение | Откуда |
|---|---|---|
| Численность 2025-08 | 100 | 100 человек в базовом месяце |
| Численность 2026-08 | 5 | остались только пятеро |
| Ушло ФЛ за год | 95 | 100 − 5 |
| Перетекло ФЛ в другие ИНН | 80 | у 80 появился новый ИНН `7700000002` |
| ИНН перетока | 7700000002 | единственный приёмник |
| Тип перетока | массовый переход | 80 ≥ 10 и 80/100 = 80% ≥ 20% |
| Реальная потеря ФЛ за год | 95 | нетто; из них 80 объяснены перетоком |

Остальные 15 человек не появились ни в одном ИНН — это уход, а не переток.

## 8. Проверки, которые стоят в коде

Печатаются в прогресс, а не прячутся (`analysis.check`):

- **сходимость портфеля**: `Численность отч. = Численность базового − Ушло + Пришло`
  для каждой строки;
- **перетёкших не больше ушедших** — иначе разъехались грейны (переток по
  человеку, уход по четвёрке);
- **переезд между ГОСБ и переток вместе не превышают ухода**;
- **сверка с синтетикой** (`synth.selfcheck`): 17 сценариев с заранее записанными
  числами — массовый переход, ротация, обрыв, схлопывание совместительства,
  второй ИНН в добавок, переезд между ГОСБ, порог, два новых ИНН, нечисловой ИНН,
  ИНН с ведущим нулём, ИНН вне справочников, смена кода выплаты, отсечка
  материальности, шум соседнего месяца.

## 9. Известные расхождения и ограничения

1. **Два месяца, а не ряд.** Отчёт сравнивает ровно два среза. Человек, ушедший
   в марте и вернувшийся в июле, для отчёта не уходил. Сезонная яма (январь,
   август) по той же причине может выглядеть уходом — сравнивайте одноимённые
   месяцы, август с августом.
2. **Порог 2 500 ₽ режет реальную массу.** Сползание зарплаты ниже порога
   неотличимо от ухода: деньги идут, получателя в метрике нет. Это осознанное
   свойство метрики, а не ошибка; чувствительность проверяется прогоном с другим
   `AMT_MIN`.
3. **Смена кода выплаты выглядит уходом.** Если организация перевела выплаты на
   незарплатный код, её люди пропадут из портфеля целиком. Отличить это от
   реального ухода можно только по суммам вне списка кодов — в этом отчёте они
   не считаются.
4. **Приёмник у человека один.** Если человек ушёл из A и появился сразу в B и C,
   весь переток приписывается тому, где сумма больше. Полная картина — в
   `flow_edges_*.csv`.
5. **Один человек, два источника.** Если человек ушёл сразу из A и B к одному
   приёмнику C, он посчитан в перетоке и у A, и у B. Сумма колонки «Перетекло»
   по банку поэтому больше числа перетёкших людей.
6. **Отсечка материальности.** Организации, где ни в одном из двух месяцев нет
   `MIN_QTY` получателей, в отчёт не попадают вовсе. Итоги прогона считаются
   после отсечки.
7. **ИНН вне справочников.** У нечисловых ИНН и у ИНН, которых нет в
   `uzp_dim_company`, название берётся из ведомостей, а холдинг остаётся пустым.
8. **Ликвидация не размечена.** Отчёт не отличает переезд организации в новый ИНН
   от реорганизации с ликвидацией старого юрлица — для этого нужен
   `status_name` из `uzp_data_epk_consolidation` на обе даты.
